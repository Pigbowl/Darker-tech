# Git钩子自动修改文件参数说明

## Git钩子概念

Git钩子是Git版本控制系统中的一个强大功能，允许你在Git执行特定操作（如提交、推送等）前后自动运行自定义脚本。这些脚本存储在项目的`.git/hooks`目录中。

## pre-commit钩子

`pre-commit`是最常用的钩子之一，它会在你执行`git commit`命令后、实际创建提交前自动运行。这使得它成为在提交代码前执行检查或修改的理想位置。

对于你的需求 - 在每次推送代码前修改特定文件的参数，我们可以使用`pre-commit`钩子来实现：

1. 每次提交前自动运行脚本
2. 脚本修改指定文件中的特定参数
3. 修改后的文件将包含在当前提交中
4. 这样每次推送的代码都会包含更新后的参数值

pre-commit钩子比pre-push钩子更适合这种场景，因为：
- 确保每次提交都包含更新的参数
- 避免在推送时才发现参数需要更新
- 允许在本地测试修改后的代码

## 实现方式

我们将创建一个pre-commit钩子脚本，它会：
1. 识别需要修改的特定文件
2. 定位并更新特定参数
3. 将修改后的文件添加到暂存区

接下来我们将创建这个钩子脚本并进行配置。